<div class="sticky top-0 z-40 flex justify-center pointer-events-none pt-3 pb-2 bg-gradient-to-b from-creme via-creme/95 to-transparent">
  <button data-action="click->mobile-filters#open"
          class="pointer-events-auto relative flex items-center gap-2 py-2 px-6 bg-bordeaux hover:bg-bordeaux-dark text-white text-sm font-medium rounded-full shadow-md transition-colors">
    <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 20 20">
      <path fill-rule="evenodd" d="M3 3a1 1 0 011-1h12a1 1 0 011 1v3a1 1 0 01-.293.707L12 11.414V15a1 1 0 01-.293.707l-2 2A1 1 0 018 17v-5.586L3.293 6.707A1 1 0 013 6V3z" clip-rule="evenodd"/>
    </svg>
    <%= @filtered ? "Agenda filtré" : "Filtrez l'agenda" %>
    <% if @filtered %>
      <span class="absolute -top-1 -right-1 w-3 h-3 bg-moutarde rounded-full border-2 border-bordeaux" aria-label="Filtre actif"></span>
    <% end %>
  </button>
</div>
